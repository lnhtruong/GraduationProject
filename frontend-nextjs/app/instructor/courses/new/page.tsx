import CourseFormPage from "@/features/instructor/course-management/CourseFormPage";

export const metadata = { title: "Tạo khóa học — Teacher Mode" };

export default function Page() {
  return <CourseFormPage />;
}
