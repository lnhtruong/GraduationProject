import CoursesPage from "@/features/instructor/components/courses/CoursesPage";

export const metadata = { title: "Quản lý khóa học — Teacher Mode" };

export default function Page() {
  return <CoursesPage />;
}
