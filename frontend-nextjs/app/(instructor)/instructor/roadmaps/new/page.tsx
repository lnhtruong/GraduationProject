import RoadmapCreatePage from "@/features/instructor/roadmap-management/RoadmapCreate";

export const metadata = { title: "Tạo lộ trình — Teacher Mode" };

export default function Page() {
  return <RoadmapCreatePage />;
}
