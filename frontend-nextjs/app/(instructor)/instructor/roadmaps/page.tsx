import RoadmapListPage from "@/features/instructor/roadmap-management/RoadmapList";

export const metadata = { title: "Quản lý lộ trình — Teacher Mode" };

export default function Page() {
  return <RoadmapListPage />;
}
